interface:
  display_name: "SecLab App Icon"
  short_description: "为 SecLab 应用、桌面和套件入口生成统一 PNG 图标。"
  brand_color: "#1D63ED"
  default_prompt: "使用 $seclab-app-icon 为这个 SecLab 应用或套件入口制作可直接交付的 PNG 图标。"
policy:
  allow_implicit_invocation: true
